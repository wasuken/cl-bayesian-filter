#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '(:cl-bayesian-filter :cl-json :mylib) :silent t)
  )

(defpackage :ros.script.test.3810245326
  (:use :cl))
(in-package :ros.script.test.3810245326)

(defun main (&rest argv)
  (declare (ignorable argv))
  (handler-case
	  (asdf:test-system :cl-bayesian-filter)
	(error (e)
	  (format *standard-output* "~&Error: ~A~%" e)
	  (uiop:quit -1))))
;;; vim: set ft=lisp lisp:
